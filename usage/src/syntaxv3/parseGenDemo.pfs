import fs from "fs";

var csvContent = fs.readFileSync("./customer/1.csv");

generated function fizzBuzz(Int number) : `
generic fizzbuzz function that returns fizz or buzz depending on the number input
`;

for (var i = 0; i < 10; i++) {
    console.log(fizzBuzz(i));
}
