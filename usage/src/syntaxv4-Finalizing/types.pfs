number x = 2;
string y = 2;

number z = null;

z = x + y;

console.log(x, y, z);

function hello(x, number y, string z) {
    console.log(x, y, z);
}
hello(1, 2, 3); //3 is implicitly converted to string
hello(1, "2", 3) //Throws error: 2 is not a number